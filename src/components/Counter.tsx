import styled from "styled-components";



const WrapperStyles = styled.div``;


export const Counter = (props) => {

    console.log(props);

    return (
        <WrapperStyles>
            Hello Counter
        </WrapperStyles>
    )
}